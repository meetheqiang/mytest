冒泡排序
时间复杂度O（n^2），空间复杂的O（1）
void BubbleSort（int ary[], int len）
{
    if(ary == NULL || len < 2)
        return;
        
    for(int j =  len-1; j >0; --j){
        for(int i = 0; i <j; ++i){
            if(ary[i] > ary[i+1])
                swap(ary, i, i+1);
        }
    }
}
